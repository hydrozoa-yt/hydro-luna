package game.npc.combat

import api.plugin.dsl.plugin

plugin {
    name = "NPC combat"
    description =
        """
        Handles combat between the player and an NPC.
        """
    version = "1.0"
    authors += "hydrozoa-yt"
}
